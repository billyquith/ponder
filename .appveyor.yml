version: 1.0.{build}
os: Visual Studio 2015

build_script:
- cmd: mkdir build && cd build && cmake .. && cmake --build . --config Debug && cmake --build . --config Release

test_script:
- cmd: cd test && ctest -C Debug -V && ctest -C Release -V
